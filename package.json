{
  "name": "finance_simulators",
  "version": "1.0.0",
  "description": "Funções de cálculos de finanças",
  "author": "Mateus Resende <mateus.rsnd@gmail.com>",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest lib/**/*.test.js",
    "watch": "mocha --watch lib/**/*.test.js",
    "debug": "mocha --watch lib/**/*.test.js --debug-brk --inspect",
    "lint": "standard"
  },
  "devDependencies": {
    "standard": "^16.0.4"
  },
  "standard": {
    "globals": [
      "describe",
      "test",
      "expect"
    ]
  },
  "dependencies": {
    "business-days-calculator": "^2.2.0",
    "holidays-calendar-brazil": "^1.0.2",
    "jest": "^27.2.5"
  }
}
